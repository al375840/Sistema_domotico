//import {initializeTestBed, limpiarEstado, obtainRoomService} from './comun';
import {take} from 'rxjs/operators';

describe('HUH01: Crear dispositivo de los distitos estados', () => {

  beforeEach(() => {
    //initializeTestBed();
  });

  it('Deberia poder añadir dispositivos con un tipo correcto', async () => {
    //Given un tipo correcto de dispositivo.
    // When el usuario lo crea.  
    //Then se registra el dispositivo del tipo requerido.
    
  });

  it('No deberia poder añadir dispositivos con un tipo no valido', async () => {
    //Given  un tipo incorrecto de dispositivo. 
    //When el usuario lo crea.  
    //Then no se registra la habitación.

  });

  afterEach(() => {
    //limpiarEstado();
  });
});