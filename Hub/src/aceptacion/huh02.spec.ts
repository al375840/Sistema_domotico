//import {initializeTestBed, limpiarEstado, obtainRoomService} from './comun';
import {take} from 'rxjs/operators';

describe('HUH02: Modificar el estado del dispositivo', () => {

  beforeEach(() => {
    //initializeTestBed();
  });

  it('Deberia poder modificar con un identificador y un nuevo estado valido', async () => {
    //Given el identificador del dispositivo y un estado válido.
    //When el usuario lo modifica.  
    //Then se realiza el cambio.
    
  });

  it('Deberia no poder modificar con un identificador y un nuevo estado no valido', async () => {
    //Given  el identificador del dispositivo y un estado inválido.
    //When  el usuario lo modifica.
    //Then  se realiza el cambio.

  });

  afterEach(() => {
    //limpiarEstado();
  });
});