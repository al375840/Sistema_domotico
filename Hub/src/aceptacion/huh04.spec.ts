
import {take} from 'rxjs/operators';

describe('HUH04: Desconectar HUB', () => {

  beforeEach(() => {
    //initializeTestBed();
  });

  it('Deberia poder desconectar el HUB', async () => {
    //Given el HUB conectado al servidor 
    //When el usuario lo desconecta.  
    //Then se desconectará del servidor.
    
  });

  it('Deberia poder conectar el HUB', async () => {
    //Given  el HUB desconectado al servidor 
    //When  el usuario lo conecta
    //Then se conectará al servidor.

  });

  afterEach(() => {
    //limpiarEstado();
  });
});